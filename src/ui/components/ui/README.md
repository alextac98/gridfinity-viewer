# UI Components

Shared app-neutral interface primitives belong here.

Keep generator-specific controls inside their owning `src/ui/apps/<app-name>/`
folder unless at least two apps need the same component.
